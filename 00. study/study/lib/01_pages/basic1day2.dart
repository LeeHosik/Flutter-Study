import 'package:flutter/material.dart';

class Basic_02 extends StatelessWidget {
  const Basic_02({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text(
          'Basic 01',
        ),
      ),
    );
  }
}
